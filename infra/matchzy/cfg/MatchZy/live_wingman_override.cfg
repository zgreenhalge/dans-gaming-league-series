// DGLS games engine-detect as Wingman mode at go-live (confirmed via mp_freezetime live-check:
// 10, matching packaged live_wingman.cfg, not the 15 in live_override.cfg), so this is the file
// MatchZy actually execs for real matches. Point it at the same baseline as live_override.cfg
// rather than maintaining two copies.
exec MatchZy/live_override.cfg
